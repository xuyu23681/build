# .github/workflows/build.yml
name: Armbian RK3288 TVBox Build (Firefly Compatible)

on:
  push:
    branches: [ main ]
    paths:
      - 'userpatches/**'     # 只在改 DTS/overlay 时自动触发
      - 'config/boards/**'   # 如果自定义板
      - '.github/workflows/**'
  workflow_dispatch:        # 手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repo (with custom DTS)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Armbian Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y git build-essential debootstrap qemu-user-static binfmt-support python3-pip ccache flex bison libssl-dev device-tree-compiler bc kmod cpio unzip rsync

      - name: Clone and Run Armbian Compile Script
        run: |
          git clone --depth=1 https://github.com/armbian/build
          cd build
          
          # 复制你的自定义文件（DTS overlay 等）
          cp -r ../userpatches ./userpatches || true
          cp -r ../config ./config || true  # 如果有自定义板
          
          # 调试：输出参数确认
          echo "Running compile.sh with params: BOARD=rk3288-tvbox BRANCH=current RELEASE=bookworm BUILD_MINIMAL=no BUILD_DESKTOP=no KERNEL_ONLY=no KERNEL_CONFIGURE=no OVERLAYS=rk3288-custom-overlay COMPRESS_OUTPUTIMAGE=sha,img,xz EXPERT=yes USE_CCACHE=yes"
          
          # 单行命令：用官方 RK3288 TVBox BOARD（兼容 Firefly），加载你的 overlay
          ./compile.sh BOARD=rk3288-tvbox BRANCH=current RELEASE=bookworm BUILD_MINIMAL=no BUILD_DESKTOP=no KERNEL_ONLY=no KERNEL_CONFIGURE=no OVERLAYS="rk3288-custom-overlay" COMPRESS_OUTPUTIMAGE=sha,img,xz EXPERT=yes USE_CCACHE=yes

      - name: Upload Artifacts (Image + Logs)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Armbian-RK3288-${{ github.sha }}
          path: |
            build/output/images/*.img*
            build/output/logs/*.log
            build/output/debug/*.txt
          retention-days: 7
